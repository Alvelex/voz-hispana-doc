# src

Este repo está pensado principalmente para documentación manual en `docs/`.

Si más adelante se quieren generar páginas de API con Moonwave desde código Luau, se pueden añadir módulos documentados en esta carpeta.
